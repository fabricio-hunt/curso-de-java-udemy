/* public class TesteDeMesa {
    public static void main(String[] args) {
        /*O teste de mesa é uma técnica utilizada para simular a execução de um programa ou algoritmo "manualmente",
        ou seja, sem o uso de um computador. É uma forma de acompanhar o passo a passo do fluxo de execução do código,
        analisando valores de variáveis, condições e saídas esperadas. É muito útil para entender o comportamento do programa e identificar erros lógicos.

Como realizar o teste de mesa?
Crie uma tabela com as seguintes colunas:

Passo (ou linha do código sendo executada).
Variáveis (com seus valores a cada etapa).
Saída (caso o programa exiba algo no console ou produza um resultado).
Leia o código linha a linha, simulando a execução e preenchendo a tabela.

Atualize os valores das variáveis conforme o fluxo do programa.

Registre a saída sempre que o programa exibir algo (ex.: System.out.println()).
        public class TesteDeMesa {
            public static void main(String[] args) {
                int a = 5;
                int b = 10;
                int soma = 0;

                for (int i = 0; i < 3; i++) {
                    soma += a + b;
                    System.out.println("Iteração " + i + ": soma = " + soma);
                }
            }
        }

    }
}
*/